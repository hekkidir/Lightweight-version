# ── Generated / live-updating data (rebuilt by the pipeline every run) ──
# tickers.csv is the hand-edited seed input and IS tracked (it's .csv, not parquet).
data/*.parquet
# robots.json is produced by the robots stage (or seeded) — runtime data, not source.
data/robots.json
.server.log

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# ── Secrets / local env ──
.env

# ── Editor / OS ──
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── Pipeline logs / scratch ──
*.log
_test_*.py
